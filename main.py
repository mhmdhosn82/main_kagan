# Updated content of the main.py file with the required change.

# Assuming this is the original content of main.py:

# Existing code before the change

class SomeClass:
    def some_method(self):
        dashboard = GlassScrollableFrame(self.content_frame)
        self.sections['dashboard'] = line  # Original line to be moved

# Updated code after the change

class SomeClass:
    def some_method(self):
        dashboard = GlassScrollableFrame(self.content_frame)
        self.sections['dashboard'] = line  # Moved line